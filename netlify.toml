# Netlify build configuration.
# Astro static output → dist/. Netlify auto-detects Astro, but this makes the
# build command, publish dir, and Node version explicit and reproducible.
[build]
  command = "astro build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
